---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - ./inventory

  execution-environment:
    image:  quay.io/tmichett/do457-custom-ee:2.0
    pull:
      policy: missing
  
  mode: stdout

  playbook-artifact:
    enable: false
