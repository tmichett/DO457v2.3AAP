---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - ./inventory

  execution-environment:
    image:  quay.io/tmichett/travis_do374:1.1
    pull:
      policy: missing
  
  mode: stdout

  playbook-artifact:
    enable: false
